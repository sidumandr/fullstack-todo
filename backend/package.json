{
  "name": "todo-app-backend",
  "version": "1.0.0",
  "description": "Todo App Backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore 'node_modules/*' --ignore 'package-lock.json' --ignore '*.test.js' --ignore '*.json' --ignore '*.env' --ignore 'routes/*' --ignore 'models/*' server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "mongoose": "^8.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
